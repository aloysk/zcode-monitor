# 遗留项统一登记（residuals）

- 建立：2026-09-22（全量审查 R1 加固轮）。此前遗留事项散落在各验收文档与评审
  记录里，无统一追溯点——本文件是唯一登记处：新遗留入册，已解决的销账并注明
  证据，不删除条目（保留审计轨迹）。
- 互链：实施留痕见 `docs/plans/ecosystem-adoption-v1.md`；各项验收记录见本目录。
- 2026-09-23（R2 加固轮）：更正 R-2 销账理由（原举例失实），增补 R-8～R-10。
- 2026-09-23（R3 加固轮）：改写 R-9 机制描述（原「逐 JSONL 现算」失实），增补 R-11～R-13。
- 2026-09-23（R4 双轮加固轮）：无新增登记，既有条目无状态变化。该轮落地 raw/sessions/trace/transcript 行数参数全量钳界（负值原为 SQLite 无上限 LIMIT，真实库实测最长 8.8s 同步冻结）与 slowTools 候选集规模钳制（rowid 尾界 ≤10 万行 + 30d 双保险，UI 注明口径）；R-13 的自由 where 亚秒级残余维持原判。
- 2026-09-23（快照绊线轮）：新增快照绊线功能（`server/snapshot-watch.js`，一轮五视角对抗评审后合入）；R-5 隐私横幅截图补拍销账；增补 R-14（绊线已知边界）。
- 2026-09-25（生态采纳 batch1 六席终审修复轮，第 1 轮 25 条）：usage.js 取数失败兜底（failCard+先置 loading，杜绝旧数据挂新窗标签）/attribution 空窗明细区改隐藏（删后不重建的永久消失）/水位 live 防重叠闸改 rowid 行序（勘误「经流重放」注释——live.js 每连接水位=连接时 MAX(rowid) 不回放，started_at 时间闸误丢同毫秒真新行与晚落库长请求行）/renderContext 代际 token（跨会话竞态孤儿 EventSource）/种子取数失败与空态区分（不再折叠成「新会话无数据」）/截断标注族内收敛 meta.*（turns 加 meta.truncated 顶层留过渡；attribution turn 层省略 window/since）/attrLimit ±Infinity 回落 1/EXPLAIN 机检补 contextGauge+sessionList 两路/README·how·口径册（§12 三项新口径）·c2-human-gate 收尾补齐。增补 R-20（水位种子-连接缺口）、R-21（7399 滞留客户端盲区，环境类）、R-22（30d cap 收窄触发线）、R-23（models-meta 非官方权威）、R-24（C2-9/C5-6/C9-5 人工评审调度）。
- 2026-09-25（六席终审修复第 2 轮 9 条）：水位 live 行有界累积（GAUGE_LIVE_ROWS_CAP=2× 种子上限，丢最旧 live 行；增量曲线 maxCols 列数截断+「+k」占位——长开标签亚像素列宽与 O(n) 重渲双收口）/EXPLAIN 排除集显式化（session 页查询从机检集显式滤出，别名形态基表 SCAN 不再静默逸出）/start-detached 占口桩 teardown 三重防护（stubDead 后到即毁 + close 先停收再摧毁 + closeAllConnections 双路——R-21 挂死形态实机复现-修复-复验销账，全套门禁曾 15min+ 无限挂死）/context-gauge 行形状钉 rid 列（rowid 闸种子侧数据依赖）/turns meta.truncated 补 canonical 读法钉+HTTP true 态例/README 勘正「widget/桌宠 hover 副行」（pet.html 无此面，速度轮 H-1 同族文档层重现）/spec·plan 状态行翻已实施+§2.3 终审偏差注（turn 层省略 window/since）/R-25（by_error_type_truncated 过渡字段清理登记）。
- 2026-09-23（六视角终审轮，zcode-pr-review-toolkit）：绊线增量二次加固——子目录读失败从「折叠静默」改为 partial 分级（读不了的那拍不进差异也不进零点，双向防假告警/防吞活动）、截断拍（超规模钳界）拒绝锚零点、`__proto__` 目录名防护、slice 置位 truncated、未知 status 前端异常渲染、README 目录锁定矛盾修正（icacls 只拒写不拒读）。无新增登记；R-14 边界①～⑤为结构性边界，本轮修复不改变其成立性。
- 2026-09-23（洁癖收尾轮，neat-freak）：文档/记忆一致性核查——T6 两份验收文档补 R-2 销账指针、plans 行号时效声明与「勿重放」注记、specs §2 加「现状锚点为开工前快照」注、T2 复现路径注记、README 绊线卡补「首扫中」瞬时态。无行为变更，无新增登记，既有条目无状态变化。
- 2026-09-23（workflow_child 计数轮，fix/workflow-child-accounting）：实证并修复动态工作流（dwf）actor 在全面板被漏计（24h 窗口 ≈17% 请求、2415/14176 次）——速度卡/速度表徽标与会话三态徽标/筛选下拉/How 文档分列纳入，raw 白名单+UI 下拉补 dwf_* 运行册表，顺带修复 overview 切页后 SSE 推送撞 null innerHTML（renderFeed 自愈，实测每分钟 47 条 TypeError）。三子代理对抗评审（代码/兼容性/测试质量）无必修逻辑缺陷，M-1（raw 页下拉缺新表）已修。增补 R-15（本项，已销账）与 R-16（dwf raw 面条件性残余）；UI 取证截图 `workflow-child-accounting-{overview,sessions-detail,how}.png` 入库本目录。
- 2026-09-23（重启功能四席评审加固轮·第 3 轮终审，fix/restart-gauntlet）：并发/测试席判 MERGE、失败模式/安全席 FIX-FIRST 各一小项，全部修毕——①SEC-006（安全席实测复现）：终端消毒器曾注册在 SPA fallback 之前，Express 错误处理器只罩先注册者，fallback 的 sendFile 错误（恶意 Range 头 416）仍落 finalhandler 含栈响应 → 调换注册序（消毒器最后），7399 实测 416 从含栈 600B+ 变 54B 通用 JSON（SPA 与静态面同罩），装配契约加「消毒器晚于 SPA」钉与「禁全局 body 解析」负钉；②跨代守卫是装饰性恒真式（currentChild 为请求局部变量，child !== currentChild 永假）→ 提升工厂级 + 跨代回归钉（上一代迟到 exit 不得回退当代受理闩）；③早夭日志带 code/signal（区分崩溃与主动退出的唯一现场）；④README「接替失败 500」限定为同步形态；⑤计数口径更正并随第 3 轮新增跨代用例定稿 11 例。R-17 补两条已接受边界（事件循环饥饿竞窗 a / 旧进程运行期 stderr 在 companion 模式随 NUL 丢 b）。
- 2026-09-23（重启功能四席评审加固轮·第 2 轮，fix/restart-gauntlet）：四席复核第 1 轮七项修复全部判 SOUND，另修——①child 早夭 `'exit'`（部署秒崩新代码，主用例）此前不撤退出定时器，头注「绝不出现答应了却谁都没起来」存在第三种失效形态 → 补 exit 守卫（250ms 旧退出 < 600ms 接替 listen，撤销必然安全）+ 跨代 currentChild 防护 + 回归钉；②旧进程侧失败诊断在壳自有（companion）模式随 NUL 消失 → noteFailure 双写 appendFileSync 进 restart-child.log（用户唯一会 grep 的地方）；③EADDRINUSE 重试预算 10s < 本仓自证冷阻塞上界 ~60s → 提到 65s；④壳三态探测（Up/Refused/Blocked）——Blocked（活着但事件循环卡死）不再误判为 down 触发 ensure 双拉起必败竞速者，改为记日志放弃本轮；⑤res.json 断链守卫（防「闩已置、退出未武装」搁浅）；⑥restart-child.log >1MB 时截断重开（SEC-005 无界增长：任意网页可跨站 POST 垃圾 JSON 触发 stderr 栈）；⑦安全席 SEC-004：express.json 收窄到 /api 作用域 + 终端错误消毒器（4xx/5xx 都不回栈）——非 /api 路径的含栈泄露面闭合（7399 实测：恶意 Host POST /widget 从 1088B 含栈页变 146B 404，合法畸形 JSON 回通用 bad_request）；⑧README 补可用性级披露（同源脚本可触发重启）。测试 8→10 例（早夭 exit 撤销/跨代防护/stderr 回退不阻断/首部值 "1" 跨语言钉/65s 预算钉/三态探测钉/microtask 确定性——另有装配契约 3 钉与文件级常量断言不计入用例数），装配契约改钉 /api 作用域解析器 + 消毒器在翻译层后。本仓工作区教训：serena 会话恢复后活动项目回退曾把第 1 轮 residuals 编辑写进主仓工作区（已检出搬回），后续本仓编辑一律走 Edit 工具。
- 2026-09-23（重启功能四席评审加固轮·第 1 轮，fix/restart-gauntlet）：并发/失败模式/安全/测试质量四席并行对抗评审重启菜单轮（498f5e6..0a958cf）。四席独立实锤同一 CRITICAL——child 异步 `error` 只回退受理闩、不撤销已武装的退出定时器，异步 spawn 失败（ENOENT/EPERM/EMFILE）时旧进程 250ms 后照死且无接替，恰是模块头注承诺不发生的事故 → 保留句柄并在 error 处理器 clearTimeout（回归钉：EventEmitter 假 child emit error 后 exit 不得被调）。其余修复：接替 stderr 落盘 `logs/restart-child.log`（stdio 全忽略曾是 R-17 场景零观测）；index.js listen EADDRINUSE 带界重试（冷查询阻塞旧进程迟退的自愈）；壳 `_ensureBusy` 闩（Shown 双拉起竞态）+ `HttpLong` 15s（重启 POST 被 2s 探测预算误杀）+ 导航失败有界重试 3 次（错误页无脚本=菜单失联曾是终态）+ ensure 结果诚实日志 + wait-down 耗尽标注；安全席顺带闭合 SEC-002（express.json 挪到 Host 闸后，恶意 Host+畸形 JSON 不再吐 body-parser 含栈 400）；tooltip 去掉「几秒内自动恢复」过度承诺。测试 6→8 例 + 装配契约 2 行（restart 在闸后/解析器在闸后）+ 壳源码契约（首部字面量/双闩/早退/导航重试/长超时）。
- 2026-09-23（重启菜单轮，feat/widget-restart-menu）：桌宠右键菜单补「重启面板」（用户会话内直接提出）——服务端新增 `POST /api/restart`（自定义首部闸 `X-Zcode-Monitor-Restart`，`server/restart-route.js`：spawn 接替进程→回 200→旧进程 250ms 后退出；接替进程经 `ZCODE_RESTART_BOOT_DELAY_MS=600` 延迟 listen 完成端口交接；spawn 失败 500 不退出、child 异步 error 回退受理闩可重试）；壳 `RestartServerAsync`（服务在跑 POST 端点、没在跑走 EnsureServerAsync 兜底；等旧监听消失→等新监听→重载 WebView 页面拿新前端代码；双击竞态 `_restartBusy` 闩、非 2xx 早退）。测试 `test/restart-route.test.js` 6 例（首部闸/spawn 位置参数契约——7399 实机冒烟抓出单对象形态 bug/幂等闩/spawn 抛错 500 可重试/GET 404/index.js 延迟 listen 源码契约）；7399 真重启循环实机验证（旧 PID 退出→接替 PID 绑定→健康）；对抗 code-review 无 critical/major，两项必修（非 2xx 早退、child error 事件）已修，竞态闩与顶层 catch 顺带收掉。登记 R-17（接替进程 boot 失败静默，已知取舍）。
- 2026-09-23（workflow_child 六视角终审轮，zcode-pr-review-toolkit）：注释/测试/静默失败/类型设计/总质量/简化六视角并行评审 → 修复——How 页示例行跨模型组求和（by_model 按 provider/model 分组，find() 只取首组曾少报 41% 且与同卡正文矛盾）；raw 白名单放行但本库不缺的表从 Express 默认 500 HTML 改 400 可读错误（R-16② 销账，顺带消除与 UI 下拉可达性的登记矛盾）；会话列表未知 task_type 不再冒充 main 徽标（selection_side_chat 真实库存量反例）；startLive 三处竞态加固（入口幂等 close/onerror 区分服务端 error 帧/畸形帧解析日志）+ renderKpis/renderSeries/renderBreakdown 补切页自弃守卫；徽标映射收敛 SRC_COLOR 单点 + childBadge 小函数（task_type 优先）；R-15 补截图隐私声明。测试补强至 5 例（order=sequence 不回落/窄窗 NULL→0/缺表 400/SSE 守卫含 return/startLive 幂等 close），全套 179 用例绿（node --test 直列计数口径）。

## 登记项

| # | 事项 | 状态 | 说明与证据 |
|---|---|---|---|
| R-1 | A1-8 与 A4-7 的壳内侧（WebView2）人工实机评审 | **未决（human-gate）** | 自动化可验证项全绿，但两验收点的实机演示未执行：A1-8（真实包导入→图鉴可见→轮换可达）见 `T2-import-e2e.md`；A4-7（壳内拖动/连击/滚轮/帧率）见 `T5-behavior-e2e.md`。两文件均含复现步骤，不代填结论。 |
| R-2 | `todayLogFile()` 的 UTC 日映射读侧缺陷（本地 00:00–08:00 读不到新行） | **已销账（本轮）** | 实测依据 `T6-latency-samples.md` §4。本轮把 `tailLog` / `eventsForTrace` 一并切到 `defaultTodayFile` 的「名字最新」语义（对 UTC/本地命名都成立），watch 缺省早已如此；`todayLogFile()` 导出保留 UTC 映射仅为兼容仓外既有调用面——仓内已无读路径依赖它（R2 核对：`tools/log-latency-probe.js` 的 mini-tail 本就跟随 `defaultTodayFile`；`server/routes/live.js:18` 仅存一处赋值、未被读取）。测试：`test/log-tail-behavior.test.js` 跨本地午日用例。 |
| R-3 | win32 CLI 形态探测盲区 | **未决（有兜底）** | `tasklist` 探测只认桌面端镜像名 `ZCode.exe`，`node` 跑的 CLI 形态探测为「未运行」。兜底：`-wal` 静默窗口（`walIdleMs < 60s` = 真实 writer 在场）在自动 checkpoint 前否决误报（`server/zcode-runtime.js`）；本轮把同一否决加到 `/api/checkpoint` 执行前（即时判据，不再只依赖最长 ~37s 陈旧的进程探测结果）。根治（识别 CLI 形态进程）无必要不做。 |
| R-4 | UI 视觉发现的追溯缺口 | **未决（需人工看图）** | R1 评审报告称存在 3 项视觉发现未入库，并引用了 dashboard/pets/widget/pet 四页的截图复核结论（pets 布局、对比度 5.76:1 达 AA 等）——这些结论未在本仓留痕，本轮无法核实或复现。代码级已核事实：`public/widget.html`/`public/pet.html` 的 `--notice` 浅色档注释声明 #b35600 对白底 ≈4.9:1（AA）。销账条件：人工看图复核四页并把结论入库。 |
| R-5 | dashboard 隐私横幅的截图取证缺口 | **已销账（快照绊线轮）** | 补拍默认展示态截图：`docs/acceptance/privacy-banner-default.png`（2026-09-23，Playwright 全新浏览器 profile——无关闭记录，横幅默认展示；文案为该轮升级后的本机实证三分措辞）。代码级事实不变：横幅默认展示，关闭写 `localStorage zc-privacy-notice-dismissed` 后不再出现。 |
| R-6 | 「锚定无测试」的过时说法 | **已销账** | 该说法早于 `test/log-tail-behavior.test.js` 的 A3-watch 锚定用例（EPERM 补锚定、历史不回放）；本轮又补 A3-7（截断重读）与 A3-8（文件名回退/回归不回放）。后续引用以测试文件为准。 |
| R-7 | 许可证未知包的导入语义（A1-4 修订） | **已实施（本轮，语义变更留痕）** | 原 Spec A1-4 为「许可证缺失仅警告、导入不阻断」；本轮改为缺省拒绝、显式确认（API `ackUnknownLicense` / CLI `--ack-unlicensed`）后放行，NOTICE 免责声明同步改为中性措辞（不再自动断言「粉丝自制」——来源与授权状态未核实）。确认后仍为「成功但带警告」，A1-4 的 NOTICE 三要素断言不变。 |
| R-8 | Google Fonts 本地化（R2 低-i，驳回登记） | **未决（权衡后挂起）** | CSP 仅存的外联域。驳回理由：Noto Sans SC 是 CJK `unicode-range` 子集矩阵（100+ 个 woff2、合计 ~10MB+），全量 vendoring 体积不可接受；手工裁剪子集在动态文本（会话名、包元数据）上有豆腐块风险。现状：断网时回退 `styles.css` 的系统字体栈，功能无损。出路二选一即销账：接受系统字体为最终形态（删 `@import`），或自托管拉丁字体 + 系统 CJK。 |
| R-9 | overview 回退路径「慢但可用」（R1 低-4 驳回登记） | **已知取舍** | 机制（R3 改写，原「逐 JSONL 现算」描述失实——本端点不读 JSONL）：`server/db.js:203-221` 在 `model_usage_started_model_idx` 缺失（旧版 ZCode / 外部 `ZCODE_DB`）时回退到不加 `INDEXED BY` 的原查询，真实库实测单次 1.4-1.8s 全索引扫、同步阻塞事件循环（INDEXED BY 路径 0.6ms、结果一致）。R1 低-4 的 TTL 缓存建议被驳回（回退是异常路径、低频；缓存会掩盖恢复时刻的陈旧窗口，正确性优先于回退速度）。接受残余：触发回退期间该端点慢，但可用性与新鲜度正确。 |
| R-10 | log-tail 换名回归丢行（R2 低-c） | **已销账（R2）** | `readOffsets` Map（名字→语义未读偏移，截断取 min）替代「读过」集合：回归到已读文件从记录偏移续读，锚定后新写入不丢。测试：`test/log-tail.test.js` A3-8。有意保留的窄窗取舍：删除/复活（高-B、低-d）按「复活即当刻 size 补锚定」，复活瞬间已存在的行不追认——避免整文件回放，代码注释逐处标明。**R3 更新**：复活用例（i:4）已改为 A3-8 式重试追加——被读过的文件删除重建后从已读偏移续读、窗口内写入补投递（`test/log-tail-behavior.test.js` 复活重试追加用例）；从未读过的文件仍按 size 锚定不回放。 |
| R-11 | checkpoint 折叠 I/O 时长不受 busy_timeout 约束（R3 登记low-①） | **未决（接受残余）** | `wal_checkpoint(TRUNCATE)` 是同步 better-sqlite3 调用：busy_timeout 只约束**锁等待**，不约束折叠 WAL 的磁盘 I/O——巨大 `-wal`（数百 MB，异常退出遗留）时事件循环可被秒级冻结。预检体积改 PASSIVE/延后均不成立：PASSIVE 不清空 `-wal`，达不到「ZCode 退出后新只读连接可见全史」的存在目的；延后=大 WAL 永不折叠，正中要害场景。缓解现状：自动 checkpoint 仅在 ZCode 退出且 `-wal` 静默 ≥60s 后触发，且正常退出路径 ZCode 自身已折叠 WAL——残留窗口仅「异常退出 + 巨大 WAL」的罕见组合。可选后续缓解：折叠前 `-wal` 体积预检超阈值时 console.warn 提示（不改变行为，只留观测面）。 |
| R-12 | staging 硬链接可绕包含性不变量（R3 登记low-②） | **未决（声明不在防御面）** | `auditNoSymlinks` 用 lstat，硬链接在 lstat 视角是普通文件——staging 内指向卷外文件的硬链接对词法/真实路径包含性审计均不可见，其内容可随导入落入 `public/pets`。危害有限：创建硬链接需本地写权限（面板威胁模型外的本地攻击者）；内容面受 sheet 32MB 上限、webp 头校验、`/pets` 非图片强制下载与 `/api` 回环闸约束（声明见 `server/pet-import.js` auditNoSymlinks 注释）。深度防御（内容寻址/来源标记）成本高于残余风险，不做。 |
| R-13 | raw 查询面的亚秒级残余（R3 必修-2 伴随登记） | **已知取舍** | UI 可达组合（13 表 × 下拉 5 order 值 × desc，真实库实测 130 组合最慢 71ms）已全部达标；自由 where 输入框的残余面：非巨表（model_usage 38.6万/tool_usage 52.3万/session_entry 11.7万行）上前缀 LIKE 或无索引列谓词在**无命中**时全表扫 0.2-0.7s（实测 model_usage 285ms / tool_usage 冷态 724ms）——亚秒级、一次性 debug 动作，接受；巨表（message/part）的 LIKE 与无索引列谓词已直接 400（实测无命中前缀 LIKE 在 part 上 61.7s 冷/8.9s 热，不容许）。 |
| R-14 | 快照绊线的覆盖边界（快照绊线轮登记） | **已知取舍** | ① 只在面板运行时段设防：停机期间机制复活以启动后的「遗留静止」态呈现（卡上「最后活动」相对时间辅助判断，UI tooltip 已注明零点语义）；② 面板重启重置零点——闩锁告警随重启消失（真实告警证据同时消失，处置前先自行留痕）；③ 纯轮询降级（fs.watch 不可用/出错）时检测延迟上界 = 轮询周期 30s；watch 存活时 ≈3s 去抖+扫描时长；④ 理论漏报窗口：快照「落盘→上传→清理」全部发生在一拍之间且连 state.json 都不残留时纯扫描差异不可见——本机实证（2026-09-23，21 个 state.json 在上传被接受后仍滞留）表明真实上传必留 state.json，按证据接受；⑤ 顶栏 chip fail-safe 取向为「宁可不亮不误报」：`/api/snapshot` 持续不可达时告警面只剩绊线卡自身。缓解现状均写入 `server/snapshot-watch.js` 头注与 README「快照绊线」节；watch 异步错误已挂 error 监听降级（不再崩进程），不可读目录单列 unreadable 态（不伪装静默）。 |
| R-15 | workflow_child（动态工作流 actor）全面板漏计（workflow_child 计数轮登记） | **已销账（本轮）** | 实证（2026-09-23 直查真实库）：dwf actor 是独立 session 行（`task_type='workflow_child'`、有 `parent_id` 挂父会话、`query_source='workflow_child'`，运行册真身在 `dwf_run`/`dwf_actor`——`workflow_run`/`workflow_activity` 为空表），此前速度计数/徽标/筛选/How 文档只认 `subagent`/`subagent_child`，24h 窗口漏计 ≈17% 请求。修复：`overviewSpeed` 分列返回 `workflow_child_count`（API additive，`subagent_count` 语义不变）；overview 徽标 purple 三态+footer 工作流计数；sessions 列表/详情/子 Agent 表三态徽标与筛选选项；how.js 四类 task_type+×N 在飞口径概念；raw 白名单+UI 下拉补 dwf_*/workflow_* 八表与 sequence 排序。测试：`test/workflow-child-accounting.test.js`（口径分列不对称探针/挂链前提含 taskType 接线/raw 四表放行+400 兜底/前端源码契约含 SSE 自愈守卫），全套 191 用例绿。UI 取证：本目录三张截图。widget ×N（lanes）本就按在飞会话去重不分类、无漏计，口径差异已在 How 页文档化。**六视角终审增补（隐私声明）**：三张取证截图含真实会话标题/prompt 片段/token 计数/已装 MCP 服务器名，按 R-5 先例接受随公开 fork 推送（2026-09-23 会话中已向用户披露、未表异议）；how.js 源码在更早版本即含同款真实数据，暴露面非本轮新开。 |
| R-17 | 「重启面板」接替进程失败启动时无用户可见信号（重启菜单轮登记；首轮四席评审后收窄） | **已知取舍** | 壳右键菜单「重启面板」→ `POST /api/restart`（`server/restart-route.js`）：受理响应必然先于接替进程 boot（端口交接时序），端点无法在 250ms 内验证新进程存活。若新进程起不来（如部署了坏代码），桌宠停留在旧冻结页面，无告警 UI。恢复路径已内建：再次点击菜单（服务已死 → `EnsureServerAsync` 全新拉起），或手动 `npm start`。**首轮四席评审收窄后的事实基线**：① spawn 同步抛错 → 500 且旧进程不退出；child 异步 `error` → 撤销退出定时器 + 回退受理闩（旧进程继续服务、可重试——「答应了却谁都没起来」在两种失败形态下都不会发生）；② 接替进程 stderr 落盘 `logs/restart-child.log`（gitignored，崩溃栈/监听失败一次 grep 可查）；③ 接替 listen 撞 EADDRINUSE（旧进程被冷查询阻塞迟退）带界重试，预算 65s（≥ 本仓自证冷阻塞上界 ~60s，二轮 F3 收紧）；④ 壳导航落错误页（无脚本=菜单也失联）有界重试 3 次；⑤ 第 2 轮再收窄：child 早夭 exit（spawn 成功即崩）同款撤销退出——三种失败形态旧进程都不退出，头注「绝不」全部为真；旧进程侧诊断双写 restart-child.log（companion 模式 NUL 不再吞）；壳三态探测把「活着但阻塞」从 down 分支摘除。仍静默的残余窗口：接替进程活过旧进程退出（>250ms）后才死（如 boot 后业务级崩溃）——用户侧无信号，但 ② 的日志可事后追溯；壳日志亦有 `restart: server did not come back`。另有两条已接受的边界（三轮终审登记）：a) 事件循环饥饿竞窗——child 在 <250ms 秒崩且同窗冷查询阻塞循环时，已到期的退出定时器先于排队的 exit 事件执行（任何基于定时器的交棒都有此固有窗，频率极低、后果与主残余同族）；b) 旧进程自身的 stderr（消毒器 5xx 栈、[db] 警告）在壳自有 companion 模式下随 NUL 丢失——本特性只把「接替进程」的 stderr 落盘，旧进程的运行期 stderr 落盘是独立的既有缺口（boot 期 companion stderr 重定向另立特性）。**六视角终审轮增补**：壳侧失败路径已加菜单文本态反馈（重启中…/重启失败——详见 widget-run.log），剩余反馈面缺口由 R-19 承接。 |
| R-18 | 阻塞态强杀的安全闸边界：非 node 进程占住 7331 时拒绝强杀（重启阻塞态修复轮实施，六视角终审轮入册） | **已知取舍** | `KillBlockedListenerAsync` 只对 `ProcessName == "node"` 的监听者放行 `Kill(entireProcessTree)`——外来进程（同样能把面板挤成「占口不应答」形态）不杀，日志指路手动处置。代价：该形态下「重启面板」无法自动恢复（但守住误杀红线）；恢复路径=手动结束占用者后重跑 `npm run start:detached` 或再点菜单。连带已修：`already-gone` 早退改 continue（同批其余 pid 不再被跳过）；netstat 工具自身失败（null）拒动并落日志。 |
| R-19 | 壳恢复链的残余静默/观测面（六视角终审轮登记） | **已知取舍** | ① 菜单文本态（本轮已加）是最低成本反馈面，但失败明细仍在 widget-run.log——非 node 拒杀、端口不放 abort 等需人工介入的分支未弹窗（打断性 UI，留待用户反馈再议）；② `EnsureServerCoreAsync` 竞速者（对被占端口 spawn 的 node）死亡不可观测：stderr 随 NUL 丢失、无 Exited 钩子，最终只有 `server: not up after 12s` 与真因（EADDRINUSE）无关——属既有缺口，本轮阻塞恢复链扩大其可达面（WatchBlocked 把 backlog 打满的活阻塞服务误读 Refused、netstat 旁路竞态窗），修复方向=restart-route 同款 stderr 落盘 restart-child.log，另立轮次；③ start-detached 手动恢复实例不设 `ZCODE_WIDGET_CHILD`（长驻、不随壳退出）是期望语义但与 companion 谱系不同——已写入脚本头注与 AGENTS，防误收敛。 |
| R-16 | raw 面 dwf_* 表的条件性残余（workflow_child 计数轮登记，R-13 同族） | **①③ 已知取舍；② 已销账（六视角终审轮）** | ① `dwf_event` payload_json 长尾行较宽，小表默认策略下 `ORDER BY time_created DESC LIMIT 100` 安静态实测 ~20-30ms、三工作流并行重载下 ~140ms（均远低于 raw 文件头 ≤300ms 目标）；表按名硬编码进策略桶、无行数增长绊线，若涨过 BIG_TABLES 阈值（>10万行）order/前导 LIKE/COUNT 会先后越线——届时按 R-13 同法收紧。②（已销账）：老 ZCode 库（无 dwf_* 表）上 `/api/raw/dwf_*` 曾从 400 变 500（prepare 抛 no such table，`makeErrorTranslator` 只翻译锁/连接损伤类错误）——计数轮登记时写「UI 不可达」，与同轮把八表补进 UI 下拉自相矛盾；六视角终审修复：raw 路由对白名单放行但本库不存在的表（PRAGMA 列集为空）返回 400 `not present` 可读错误，测试 `workflow_definition`（fixture 天然缺表）用例锁定。③ 本轮已把 dwf_*/workflow_* 八表与 sequence 排序补进 raw 页下拉；`permission`/`session_task_link` 有意不列（敏感/边缘）。 |
| R-20 | 上下文水位 live 的「种子查询→SSE 连接」缺口（六席终审修复轮登记） | **已知取舍** | live.js 每连接水位取连接时刻的 MAX(rowid)（头注「only traffic after THIS connect streams」——回放从不发生）；(种子 GET, SSE 连接建立] 之间落库的行低于该水位：不入流也不在种子，水位可停在过期分子、增量曲线缺点，直至下一行落库或重进标签。窗口为毫秒级（种子渲染后同步建 EventSource），且随任一新行落库自愈；闭合一侧（onopen 后重取种子）需与已累积 liveRows 对账，复杂度不成比例，按证据接受。本轮已把防重叠闸改 rowid 行序（消同毫秒误丢/晚落库长请求误弃两误伤），并修正两处「经流重放」失实注释。 |
| R-21 | start-detached E2E 测试对 7399 滞留客户端连接敏感（环境类，六席终审登记；F-码-7/F-安-2/F-测-3 同源） | **挂死形态已销账（第 2 轮，实机复现-修复-复验）；端口互踩快速失败维持惯例双检** | 本机存在外部并发运行体反复活动于 7399（实证：playwright-mcp Chrome pid 51024 的 SSE 长连接、残留 node --test 进程）。挂死机制（第 2 轮实证升级：全套 npm test 从 60.5s/87.6s 挂败升级为 15min+ 无限挂死）：chrome 对 7399 的 SSE 自动重连被测试占口桩 accept，`stub.close()` 回调等这些连接关闭而永不触发，node --test 无单文件超时。**销账修复（六席终审第 2 轮 F-安-3，实机三连验证）**：占口桩 teardown 终形态＝①`stubDead` 后到即毁闸（close() 后内核 backlog 仍可能放行一条 chrome 重连、钉住测试进程事件循环——「3 例全绿但进程 2min+ 不退出」第二形态实机复现）；②先 `close()` 停收新连接再摧毁已接受连接（反序的「先摧毁后关监听」窗口内 chrome 立即重连、close 回调重新进入等待——反序实现实机复现仍挂死 45s+）；③跟踪集显式 destroy + `closeAllConnections()`（Node≥18.2）双路 + 3s 竞态兜底二扫。复验：chrome 仍在活跃重连的环境下 `node --test test/start-detached.test.js` 3/3 绿、8.9s 进程完整退出（EXIT=0）。非代码缺陷的残留面：多运行体抢占 7399 时前置断言仍快速失败（可见、非挂死）——维持治理①：门禁跑全套前 `netstat -ano | grep 7399` 双检（无 LISTENING 且无残留 ESTABLISHED/TIME_WAIT）；E2E 端口参数化（PORT=随机高位口）留后续独立小修。 |
| R-25 | turns 端点顶层 by_error_type_truncated 过渡兼容字段待清理（六席终审第 2 轮登记，F-档-10） | **已知取舍（过渡期）** | F-码-5 把截断标注收敛进 `meta.truncated`（族内统一取法，attribution 先例）后，顶层 `by_error_type_truncated` 保留为过渡兼容字段（server/routes/usage.js 分节注释「待下轮清理」）。前端已改读 canonical 形态并有源码钉（usage-view.test.js：读 `turns.meta && turns.meta.truncated` 且禁回退旧字段——清理时若前端未随迁会红）。清理触发条件：下轮任何 /api/usage 消费面变更时一并移除；销账路径：删路由顶层字段+过过渡言（usage-routes.test.js 的同值断言改为仅 meta.truncated）。 |
| R-22 | 30d 档 rowid cap=200k 的读数收窄边界（六席终审轮入册；取舍本体见 round2-batch1-explain-timing.md §1.2） | **已知取舍** | 30d 档 tool/attribution 走 rowid 尾部候选集钳制：tool_usage 550k 行被裁至 200k（30d 读数实为最近 ~8.53 天）、model_usage 407k→200k（~12.05 天）；7d 档行数超 200k 时同被收窄。UI meta.scope 如实申报（不静默）。重测触发线已入 §1.2 条目 5（7d 函数级 warm >450ms 或 7d 行数 >180k/150k 时重评 cap/加列覆盖面）；终审复测余量收窄至 6-20% 的数字照录 §1.3 末段。 |
| R-23 | models-meta 静态整理表「非官方权威」边界（六席终审轮入册） | **已知取舍** | 窗口值是静态整理表（zai-org/ZCode 官方常量逐条核对定值，头注 (a)-(e) 纪律）：GLM-5.3-FlashX 不在官方 canonical 名单、按官方 glm-5.3 规则的后缀组定值；未收录 id 一律 null（不猜窗口、不显百分比）。UI 恒标「非官方权威」（mini 条/水位区 title）；官方名单演进时本表可能滞后——发现未收录主力模型即补核对，不做自动同步。 |
| R-24 | 生态采纳 batch1 三项截图类人工评审（C2-9/C5-6/C9-5） | **未决（human-gate）** | 自动化面全绿；三项按 Global Constraint 9 留痕待人工：C2-9（models-meta 逐条核对 + mini 条/水位区/widget 副行双主题截图，`c2-human-gate.md`）、C5-6（火焰图双主题 + hover + 宽度占比手工核对，`c5-human-gate.md`）、C9-5（顶栏 chip 真机 + emptyState 截图 + 数字位核查，`c9-human-gate.md`——其 models-meta 联动抽查子项随 C2-9 同拍）。评审完成后各自在留痕文件记结论。 |
